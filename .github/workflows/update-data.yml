name: Update Gantt Data from Jira

on:
  workflow_dispatch:
  schedule:
    - cron: "0 * * * *"

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Update gantt data
        env:
          JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
          JIRA_USER_EMAIL: ${{ secrets.JIRA_USER_EMAIL }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
          JIRA_JQL: ${{ secrets.JIRA_JQL }}
          JIRA_PLAN_START_FIELD: ${{ secrets.JIRA_PLAN_START_FIELD }}
          JIRA_PLAN_END_FIELD: ${{ secrets.JIRA_PLAN_END_FIELD }}
        run: python scripts/update_from_jira.py

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/gantt-data.json
          git diff --cached --quiet || git commit -m "chore: auto-update gantt data"
          git push
